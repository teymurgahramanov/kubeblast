{{- if .Values.enabled }}
---
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.instanceName }}-secret
type: Opaque
stringData:
  MONGO_INITDB_ROOT_USERNAME: {{ .Values.username }}
  MONGO_INITDB_ROOT_PASSWORD: {{ .Values.password }}
{{- end }}