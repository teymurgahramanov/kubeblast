{{- if .Values.enabled }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.instanceName }}
spec:
  type: ClusterIP
  ports:
    - name: {{ .Values.instanceName }}
      port: 27017
      targetPort: 27017
  selector:
    app: {{ .Values.instanceName }}
{{- end }}