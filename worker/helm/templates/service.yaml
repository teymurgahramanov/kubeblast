apiVersion: v1
kind: Service
metadata:
  name: {{ include "AAA.fullname" . }}
  labels:
    {{- include "AAA.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "AAA.selectorLabels" . | nindent 4 }}
