apiVersion: batch/v1
kind: Job
metadata:
  name: {{ name }}
  namespace: {{ namespace }}
spec:
  parallelism: 1
  completions: 1
  backoffLimit: 0
  ttlSecondsAfterFinished: 172800
  template:
    spec:
      containers:
      - name: jmeter
        image: busybox
        #command: ["cat", "/tmp/{{ cm_key }}"]
        command: ["sh", "-c", "while true; do echo \"$(date): $(cat /dev/urandom | tr -dc 'a-zA-Z0-9' | head -c 8)\"; sleep 5; done"]
        volumeMounts:
        - name: jmeter-plan
          mountPath: /tmp/{{ cm_key }}
          subPath: {{ cm_key }}
      restartPolicy: Never
      volumes:
      - name: jmeter-plan
        configMap:
          name: {{ name }}
